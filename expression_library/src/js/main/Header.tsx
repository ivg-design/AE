import React from 'react';
import { Heading, Divider } from '@adobe/react-spectrum';

export default function Header() {
	return (
		<>
			<Heading level={1}>Expression Library</Heading>
			<Divider size="L" />
		</>
	);
}
